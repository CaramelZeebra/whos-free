name = "elias-unavailability"
main = "worker.js"
compatibility_date = "2024-01-01"

[[durable_objects.bindings]]
name = "CALENDAR_DO"
class_name = "CalendarStore"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["CalendarStore"]

# Set the admin token (required to create new calendars) with:
#   wrangler secret put ADMIN_TOKEN
# Do NOT put it in this file — it must stay out of the repo.
